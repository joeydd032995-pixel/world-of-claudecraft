# Game design documents

One GDD per feature, authored by `/studio-gdd` from `templates/gdd.md`. Every GDD links its
neutral mechanics spec and includes the mandatory Original-content mapping section that the
`content-provenance-auditor` checks.
