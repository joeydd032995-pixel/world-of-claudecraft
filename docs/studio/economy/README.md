# Economy models

Copper-flow models authored by `/economy-model` from `templates/economy-model.md`. Numbers
are tied to formula budgets (`xpForLevel`, vendor spreads, drop EV), never invented. All item
and currency names are original.
